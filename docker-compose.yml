services:
  postgres:
    image: postgres:16-alpine
    container_name: ncpors-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ncpors}
      POSTGRES_USER: ${POSTGRES_USER:-ncpors}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ncpors} -d ${POSTGRES_DB:-ncpors}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
